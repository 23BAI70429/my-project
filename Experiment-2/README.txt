Description: Basic Banking UI (Buttons Only)
This HTML file creates a minimal banking user interface that includes:

A simple container with two buttons:

Deposit button – to trigger deposit actions.

Withdraw button – to trigger withdrawal actions.